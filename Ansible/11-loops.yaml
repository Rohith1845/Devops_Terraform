- name: loops
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: print name
    ansible.builtin.package:
      name: "{{ item.name }}"
      state: "{{ item.state }}"
    loop:
    - { name: "nginx", state: "latest" }
    - { name: "mysql", state: "absent" }
    - { name: "zip", state: "present" }