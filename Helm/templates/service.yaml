apiVersion: v1
kind: Service
metadata:
  name: helm-demo
spec:
  type: {{ .Values.service.type }}
  selector:
    project: roboshop
    purpose: helm-demo
  ports:
    - protocol: TCP
      port: {{ .Values.service.servicePort }}  #service port
      targetPort: 80 # container port
